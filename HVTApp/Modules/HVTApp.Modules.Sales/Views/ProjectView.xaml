<inf:ViewBase x:Class="HVTApp.Modules.Sales.Views.ProjectView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:inf="clr-namespace:HVTApp.Infrastructure;assembly=HVTApp.Infrastructure"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:views="clr-namespace:HVTApp.UI.Views;assembly=HVTApp.UI"
              xmlns:controls="clr-namespace:HVTApp.Modules.Sales.Controls">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Детали проекта">
            <ScrollViewer>
                <views:ProjectDetailsView DockPanel.Dock="Top"
                                          DataContext="{Binding}"
                                          x:Name="OfferDetailsView"
                                          VisibilityManagerProject="Collapsed" 
                                          VisibilityNotesProject="Collapsed" />
            </ScrollViewer>
        </GroupBox>

        <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" Height="3" />

        <controls:SalesUnitListControl Grid.Row="2"
                                       DataContext="{Binding GroupsViewModel}"/>
    </Grid>
</inf:ViewBase>
