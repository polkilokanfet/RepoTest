<inf:ViewBase x:Class="HVTApp.Modules.Sales.Views.OfferView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:inf="clr-namespace:HVTApp.Infrastructure;assembly=HVTApp.Infrastructure"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:views="clr-namespace:HVTApp.UI.Views;assembly=HVTApp.UI"
              xmlns:controls="clr-namespace:HVTApp.Modules.Sales.Controls"
              x:Name="Root">
    
    <Grid Visibility="{Binding IsLoaded, Converter={StaticResource BooleanToVisibilityConverter}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <GroupBox Grid.Row="0" Header="Детали предложения">
            <ScrollViewer>
                <views:OfferDetailsView x:Name="OfferDetailsView"
                                        DockPanel.Dock="Top" 
                                        DataContext="{Binding}" 
                                        VisibilityAuthorOffer="Collapsed"/>
            </ScrollViewer>
        </GroupBox>
        <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" Height="3"/>
        
        <controls:OfferUnitListControl Grid.Row="2" DataContext="{Binding}"/>
        
    </Grid>
</inf:ViewBase>
