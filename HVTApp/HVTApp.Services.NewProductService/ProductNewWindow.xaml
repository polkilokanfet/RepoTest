<Window x:Class="HVTApp.Services.GetProductService.ProductNewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Создание нового продукта" 
        SizeToContent="WidthAndHeight"
        MinWidth="500">
    
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Обозначение: "/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Designation, UpdateSourceTrigger=PropertyChanged}"/>
        
        <Label Grid.Row="1" Grid.Column="0" Content="Комментарий: "/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}"/>
        
        <Button Grid.Row="2" Grid.Column="1" Content="Ok" Command="{Binding OkCommand}"/>

    </Grid>
    
</Window>
