<UserControl x:Class="HVTApp.UI.PriceEngineering.View.ProductTaskControlHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:HVTApp.UI.PriceEngineering.Converters"
             x:Name="Control">

    <UserControl.Resources>
        <converters:PriceEngineeringTaskViewModelToProductDesignationConverter x:Key="ToProductDesignationConverter" />
        <converters:PriceEngineeringTaskViewModelToFacilityConverter x:Key="ToFacilityConverter" />
        <converters:PriceEngineeringTaskViewModelToFacilityAddressConverter x:Key="ToFacilityAddressConverter" />
        <converters:PriceEngineeringTaskViewModelToProductAmountConverter x:Key="ToProductAmountConverter" />
        <converters:PriceEngineeringTaskViewModelIsExpendedConverter x:Key="IsExpendedConverter" />
        <converters:PriceEngineeringTaskViewModelManagerVisibilityConverter x:Key="ManagerVisibilityConverter" />
        <converters:PriceEngineeringTaskViewModelManagerNewVisibilityConverter x:Key="ManagerVisibilityConverterNew" />
    </UserControl.Resources>

    <Border 
        Visibility="{Binding IsVisible, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}" 
        Background="#dceaf5"  
        BorderBrush="Black" 
        BorderThickness="1" 
        CornerRadius="7">

        <Expander 
            Margin="3" 
            Header="{Binding}" 
            IsExpanded="{Binding Mode=OneTime, Converter={StaticResource IsExpendedConverter}}">

            <Expander.HeaderTemplate>
                <DataTemplate>

                    <!--Шапка-->
                    <Border 
                        HorizontalAlignment="{Binding Path=HorizontalAlignment, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContentPresenter}}, Mode=OneWayToSource}"
                        BorderBrush="Black" 
                        BorderThickness="0" 
                        CornerRadius="5 5 0 0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Grid Grid.Column="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <Label Grid.Row="0" Grid.Column="0" Content="Объект: "/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Mode=OneWay, Converter={StaticResource ToFacilityConverter}}" VerticalAlignment="Center" FontWeight="DemiBold"/>

                                <Label Grid.Row="0" Grid.Column="2" Content=" Адрес: "/>
                                <TextBlock Grid.Row="0" Grid.Column="3" Text="{Binding Mode=OneWay, Converter={StaticResource ToFacilityAddressConverter}}" VerticalAlignment="Center" FontWeight="DemiBold"/>

                                <Label Grid.Row="1" Grid.Column="0" Content="Продукт: "/>
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Mode=OneWay, Converter={StaticResource ToProductDesignationConverter}}" VerticalAlignment="Center" FontWeight="DemiBold"/>

                                <Label Grid.Row="1" Grid.Column="2" Content=" Кол-во: "/>
                                <TextBlock Grid.Row="1" Grid.Column="3" Text="{Binding Mode=OneWay, Converter={StaticResource ToProductAmountConverter}}" VerticalAlignment="Center" FontWeight="DemiBold"/>
                            </Grid>

                            <Grid Grid.Column="1" 
                                  Margin="5"
                                  Visibility="{Binding Converter={StaticResource ManagerVisibilityConverter}}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Button Grid.Row="0" Content="Удалить" Command="{Binding RemoveTaskCommand}" Padding="2" Margin="0, 0, 0, 2"
                                        Visibility="{Binding Converter={StaticResource ManagerVisibilityConverterNew}}"/>
                                <Button Grid.Row="1" Content="Синхронизировать" Command="{Binding ReplaceProductCommand}" Padding="2"/>
                                <!--<Button Grid.Row="2" Content="Произвести" Command="{Binding StartProductionCommand}"/>-->
                            </Grid>

                        </Grid>
                    </Border>

                </DataTemplate>
            </Expander.HeaderTemplate>

            <!--Проработка блоков-->
            <ContentControl 
                DataContext="{Binding}"
                Content="{Binding TasksArea, ElementName=Control}"/>

        </Expander>
    </Border>

</UserControl>
