<inf:ViewBase x:Class="HVTApp.UI.Views.ProductDetailsView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:inf="clr-namespace:HVTApp.Infrastructure;assembly=HVTApp.Infrastructure"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			  xmlns:infDP="http://infragistics.com/DataPresenter"
			  xmlns:infE="http://infragistics.com/Editors"
			  xmlns:core="clr-namespace:System;assembly=mscorlib"
              xmlns:controls="clr-namespace:HVTApp.UI.Controls"
			  x:Name="Root">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" MinWidth="200"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <Label Grid.Column="0" Grid.Row ="0" Content="Тип" Visibility="{Binding VisibilityProductTypeProduct, ElementName=Root}"/>
        <controls:ChooseItemControl Grid.Column="1" Grid.Row="0" 
                                    Item="{Binding Item.ProductType}" 
                                    AddItemCommand="{Binding SelectProductTypeCommand}" 
                                    RemoveItemCommand="{Binding ClearProductTypeCommand}"
									Visibility="{Binding VisibilityProductTypeProduct, ElementName=Root}"/>


        <Label Grid.Column="0" Grid.Row ="1" Content="Обозначение" Visibility="{Binding VisibilityDesignationProduct, ElementName=Root}"/>
        <TextBox Grid.Column="1" Grid.Row="1" MinWidth="200" 
                 Text="{Binding Item.Designation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
				 Visibility="{Binding VisibilityDesignationProduct, ElementName=Root}"/>


        <Label Grid.Column="0" Grid.Row ="2" Content="Специальное обозначение" Visibility="{Binding VisibilityDesignationSpecialProduct, ElementName=Root}"/>
        <TextBox Grid.Column="1" Grid.Row="2" MinWidth="200" 
                 Text="{Binding Item.DesignationSpecial, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
				 Visibility="{Binding VisibilityDesignationSpecialProduct, ElementName=Root}"/>


        <Label Grid.Column="0" Grid.Row ="3" Content="Блок" Visibility="{Binding VisibilityProductBlockProduct, ElementName=Root}"/>
        <controls:ChooseItemControl Grid.Column="1" Grid.Row="3" 
                                    Item="{Binding Item.ProductBlock}" 
                                    AddItemCommand="{Binding SelectProductBlockCommand}" 
                                    RemoveItemCommand="{Binding ClearProductBlockCommand}"
									Visibility="{Binding VisibilityProductBlockProduct, ElementName=Root}"/>


        <Label Grid.Column="0" Grid.Row ="4" Content="Продукты в составе" Visibility="{Binding VisibilityDependentProductsProduct, ElementName=Root}"/>
        <controls:ChooseItemsListControl Grid.Column="1" Grid.Row="4"
										 Items="{Binding Path=Item.DependentProducts}"
										 SelectedItem="{Binding SelectedDependentProductsItem, Mode=TwoWay}"
										 AddItemCommand="{Binding AddInDependentProductsCommand}"
										 RemoveItemCommand="{Binding RemoveFromDependentProductsCommand}"
										 Visibility="{Binding VisibilityDependentProductsProduct, ElementName=Root}"/>



    </Grid>
</inf:ViewBase>
