<inf:ViewBase x:Class="HVTApp.UI.Views.OfferDetailsView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:inf="clr-namespace:HVTApp.Infrastructure;assembly=HVTApp.Infrastructure"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			  xmlns:infDP="http://infragistics.com/DataPresenter"
			  xmlns:infE="http://infragistics.com/Editors"
			  xmlns:core="clr-namespace:System;assembly=mscorlib"
              xmlns:controls="clr-namespace:HVTApp.UI.Controls"
			  x:Name="Root">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" MinWidth="200"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row ="0" Content="Комментарий" Visibility="{Binding CommentVisibility, ElementName=Root}"/>
        <TextBox Grid.Column="1" Grid.Row="0" MinWidth="200" 
                 Text="{Binding Item.Comment, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
				 Visibility="{Binding CommentVisibility, ElementName=Root}"/>

        <Label Grid.Column="0" Grid.Row ="1" Content="НДС" Visibility="{Binding VatVisibility, ElementName=Root}"/>
        <infE:XamNumericEditor Grid.Column="1" Grid.Row="1" 
                               Value="{Binding Item.Vat, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
							   Visibility="{Binding VatVisibility, ElementName=Root}"/>

        <Label Grid.Column="0" Grid.Row ="2" Content="Срок действия" Visibility="{Binding ValidityDateVisibility, ElementName=Root}"/>
        <DatePicker Grid.Column="1" Grid.Row="2" 
                    SelectedDate="{Binding Item.ValidityDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
					Visibility="{Binding ValidityDateVisibility, ElementName=Root}"/>

        <Label Grid.Column="0" Grid.Row ="3" Content="Проект" Visibility="{Binding ProjectVisibility, ElementName=Root}"/>
        <controls:ChooseItemControl Grid.Column="1" Grid.Row="3" 
                                    Item="{Binding Item.Project}" 
                                    AddItemCommand="{Binding SelectProjectCommand}" 
                                    RemoveItemCommand="{Binding ClearProjectCommand}"
									Visibility="{Binding ProjectVisibility, ElementName=Root}"/>

        <Label Grid.Column="0" Grid.Row ="4" Content="Запрос" Visibility="{Binding RequestDocumentVisibility, ElementName=Root}"/>
        <controls:ChooseItemControl Grid.Column="1" Grid.Row="4" 
                                    Item="{Binding Item.RequestDocument}" 
                                    AddItemCommand="{Binding SelectRequestDocumentCommand}" 
                                    RemoveItemCommand="{Binding ClearRequestDocumentCommand}"
									Visibility="{Binding RequestDocumentVisibility, ElementName=Root}"/>

        <Label Grid.Column="0" Grid.Row ="5" Content="Автор" Visibility="{Binding AuthorVisibility, ElementName=Root}"/>
        <controls:ChooseItemControl Grid.Column="1" Grid.Row="5" 
                                    Item="{Binding Item.Author}" 
                                    AddItemCommand="{Binding SelectAuthorCommand}" 
                                    RemoveItemCommand="{Binding ClearAuthorCommand}"
									Visibility="{Binding AuthorVisibility, ElementName=Root}"/>

        <Label Grid.Column="0" Grid.Row ="6" Content="Отправитель" Visibility="{Binding SenderEmployeeVisibility, ElementName=Root}"/>
        <controls:ChooseItemControl Grid.Column="1" Grid.Row="6" 
                                    Item="{Binding Item.SenderEmployee}" 
                                    AddItemCommand="{Binding SelectSenderEmployeeCommand}" 
                                    RemoveItemCommand="{Binding ClearSenderEmployeeCommand}"
									Visibility="{Binding SenderEmployeeVisibility, ElementName=Root}"/>

        <Label Grid.Column="0" Grid.Row ="7" Content="Получатель" Visibility="{Binding RecipientEmployeeVisibility, ElementName=Root}"/>
        <controls:ChooseItemControl Grid.Column="1" Grid.Row="7" 
                                    Item="{Binding Item.RecipientEmployee}" 
                                    AddItemCommand="{Binding SelectRecipientEmployeeCommand}" 
                                    RemoveItemCommand="{Binding ClearRecipientEmployeeCommand}"
									Visibility="{Binding RecipientEmployeeVisibility, ElementName=Root}"/>

        <Label Grid.Column="0" Grid.Row ="8" Content="Исходящий" Visibility="{Binding RegistrationDetailsOfSenderVisibility, ElementName=Root}"/>
        <controls:ChooseItemControl Grid.Column="1" Grid.Row="8" 
                                    Item="{Binding Item.RegistrationDetailsOfSender}" 
                                    AddItemCommand="{Binding SelectRegistrationDetailsOfSenderCommand}" 
                                    RemoveItemCommand="{Binding ClearRegistrationDetailsOfSenderCommand}"
									Visibility="{Binding RegistrationDetailsOfSenderVisibility, ElementName=Root}"/>

        <Label Grid.Column="0" Grid.Row ="9" Content="Входящий" Visibility="{Binding RegistrationDetailsOfRecipientVisibility, ElementName=Root}"/>
        <controls:ChooseItemControl Grid.Column="1" Grid.Row="9" 
                                    Item="{Binding Item.RegistrationDetailsOfRecipient}" 
                                    AddItemCommand="{Binding SelectRegistrationDetailsOfRecipientCommand}" 
                                    RemoveItemCommand="{Binding ClearRegistrationDetailsOfRecipientCommand}"
									Visibility="{Binding RegistrationDetailsOfRecipientVisibility, ElementName=Root}"/>

        <Label Grid.Column="0" Grid.Row ="10" Content="Копия" Visibility="{Binding CopyToRecipientsVisibility, ElementName=Root}"/>
        <controls:ChooseItemsListControl Grid.Column="1" Grid.Row="10"
										 Items="{Binding Path=Item.CopyToRecipients}"
										 SelectedItem="{Binding SelectedCopyToRecipientsItem, Mode=TwoWay}"
										 AddItemCommand="{Binding AddInCopyToRecipientsCommand}"
										 RemoveItemCommand="{Binding RemoveFromCopyToRecipientsCommand}"
										 Visibility="{Binding CopyToRecipientsVisibility, ElementName=Root}"/>

        <controls:UnitListControl Grid.Row="11"
                                  Grid.Column="0"
                                  Grid.ColumnSpan="2"
                                  UnitsGroups="{Binding Groups}"
                                  SelectedUnitsGroup="{Binding SelectedGroup, Mode=OneWayToSource}"
                                  ChangeCommand="{Binding ChangeCommand}"/>



    </Grid>
</inf:ViewBase>
