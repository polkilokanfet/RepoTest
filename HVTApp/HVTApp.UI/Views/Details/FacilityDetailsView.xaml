<inf:ViewBase x:Class="HVTApp.UI.Views.FacilityDetailsView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:inf="clr-namespace:HVTApp.Infrastructure;assembly=HVTApp.Infrastructure"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			  xmlns:infDP="http://infragistics.com/DataPresenter"
			  xmlns:infE="http://infragistics.com/Editors"
			  xmlns:core="clr-namespace:System;assembly=mscorlib"
              xmlns:controls="clr-namespace:HVTApp.UI.Controls"
			  x:Name="Root">
    <Grid>
        <Grid.Resources>
            <Style TargetType="Label">
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="MinWidth" Value="200"/>
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" MinWidth="200"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row ="0" Content="Название" Visibility="{Binding VisibilityNameFacility, ElementName=Root}"/>
        <TextBox Grid.Column="1" Grid.Row="0"
                 Text="{Binding Item.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
				 Visibility="{Binding VisibilityNameFacility, ElementName=Root}"/>

        <Label Grid.Column="0" Grid.Row ="1" Content="Тип" Visibility="{Binding VisibilityTypeFacility, ElementName=Root}"/>
        <controls:ChooseItemControl Grid.Column="1" Grid.Row="1" 
                                    Item="{Binding Item.Type}" 
                                    AddItemCommand="{Binding SelectTypeCommand}" 
                                    RemoveItemCommand="{Binding ClearTypeCommand}"
									Visibility="{Binding VisibilityTypeFacility, ElementName=Root}"/>

        <Label Grid.Column="0" Grid.Row ="2" Content="Владелец" Visibility="{Binding VisibilityOwnerCompanyFacility, ElementName=Root}"/>
        <controls:ChooseItemControl Grid.Column="1" Grid.Row="2" 
                                    Item="{Binding Item.OwnerCompany}" 
                                    AddItemCommand="{Binding SelectOwnerCompanyCommand}" 
                                    RemoveItemCommand="{Binding ClearOwnerCompanyCommand}"
									Visibility="{Binding VisibilityOwnerCompanyFacility, ElementName=Root}"/>

        <Label Grid.Column="0" Grid.Row ="3" Content="Местоположение" Visibility="{Binding VisibilityAddressFacility, ElementName=Root}"/>
        <controls:ChooseItemControl Grid.Column="1" Grid.Row="3" 
                                    Item="{Binding Item.Address}" 
                                    AddItemCommand="{Binding SelectAddressCommand}" 
                                    RemoveItemCommand="{Binding ClearAddressCommand}"
									Visibility="{Binding VisibilityAddressFacility, ElementName=Root}"/>


    </Grid>
</inf:ViewBase>
