<inf:ViewBase x:Class="HVTApp.UI.TechnicalRequrementsTasksModule.TechnicalRequrementsTasksView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:inf="clr-namespace:HVTApp.Infrastructure;assembly=HVTApp.Infrastructure"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:dataPresenter="http://infragistics.com/DataPresenter">

    <Grid>
        <dataPresenter:XamDataGrid x:Name="LookupListGrid"
                                   DataSource="{Binding Lookups}"
                                   SelectedDataItem="{Binding SelectedLookup}">
        
            <dataPresenter:XamDataGrid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding Path=EditCommand, Mode=OneWay}"/>
            </dataPresenter:XamDataGrid.InputBindings>

			<dataPresenter:XamDataGrid.FieldLayouts>
				<dataPresenter:FieldLayout>
					<dataPresenter:FieldLayout.Fields>
                        <dataPresenter:DateTimeField BindingType="UseAlternateBinding" AlternateBinding="{Binding Start}" Label="Старт задачи" Width="Auto" AllowResize="False" Format="dd/MM/yy HH:mm"/>
                        <dataPresenter:TextField BindingType="UseAlternateBinding" AlternateBinding="{Binding Facilities}" Label="Объекты" Width="400" AllowResize="True" Converter="{StaticResource ObjectsToStringConverter}"/>
                        <dataPresenter:TextField BindingType="UseAlternateBinding" AlternateBinding="{Binding BackManager}" Label="Back manager" Width="Auto" AllowResize="True" Converter="{StaticResource ObjectToStringConverter}"/>
                        <dataPresenter:TextField BindingType="UseAlternateBinding" AlternateBinding="{Binding Comment}" Label="Комментарий" Width="200" AllowResize="True" />
                        <dataPresenter:TextField BindingType="UseAlternateBinding" AlternateBinding="{Binding Id}" Label="Id" Width="Auto" AllowResize="True" />
					</dataPresenter:FieldLayout.Fields>
				</dataPresenter:FieldLayout>
			</dataPresenter:XamDataGrid.FieldLayouts>
		</dataPresenter:XamDataGrid>
    </Grid>

</inf:ViewBase>