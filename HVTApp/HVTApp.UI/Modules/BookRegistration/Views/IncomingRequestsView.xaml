<inf:ViewBase x:Class="HVTApp.UI.Modules.BookRegistration.Views.IncomingRequestsView"
              xmlns:inf="clr-namespace:HVTApp.Infrastructure;assembly=HVTApp.Infrastructure"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
              xmlns:views="clr-namespace:HVTApp.UI.Views" 
              xmlns:dataPresenter="http://infragistics.com/DataPresenter">
    <Grid>
		<dataPresenter:XamDataGrid x:Name="IncomingRequestLookupListGrid" Grid.Row="1" Style="{StaticResource ListViewGrid}" Visibility="{Binding IsLoaded, Converter={StaticResource BooleanToVisibilityConverter}}">
        
			<dataPresenter:XamDataGrid.ContextMenu>
				<ContextMenu>
					<MenuItem Header="Export to Excel" Command="{Binding ExportToExcel}" CommandParameter="{Binding PlacementTarget, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ContextMenu}}}"/>
				</ContextMenu>
			</dataPresenter:XamDataGrid.ContextMenu>

			<dataPresenter:XamDataGrid.FieldLayouts>
				<dataPresenter:FieldLayout>
					<dataPresenter:FieldLayout.Fields>
                        <dataPresenter:TextField Name="Document" Label="Запрос" Width="Auto" AllowResize="True" Converter="{StaticResource LookupToStringConverter}"/>
                        <dataPresenter:TextField Name="Performers" Label="Исполнители" Width="Auto" AllowResize="True" Converter="{StaticResource LookupsToStringConverter}"/>
                        <dataPresenter:CheckBoxField Name="IsDone" Label="Исполнен" Width="Auto" AllowResize="True" />
                        <dataPresenter:CheckBoxField Name="HasAnyPerformer" Label="Поручено" Width="Auto" AllowResize="True" />
                        <dataPresenter:TextField Name="Id" Label="Id" Width="Auto" AllowResize="True" />
					</dataPresenter:FieldLayout.Fields>
				</dataPresenter:FieldLayout>

			</dataPresenter:XamDataGrid.FieldLayouts>
		</dataPresenter:XamDataGrid>
    </Grid>
</inf:ViewBase>































































