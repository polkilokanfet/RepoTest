<UserControl x:Class="HVTApp.Services.GetProductService.SelectParameter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HVTApp.Services.GetProductService"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Label Grid.Row="0" Content="{Binding Group.Name}"/>
        <ListBox Grid.Column="1" ItemsSource="{Binding Parameters}" 
                 SelectedItem="{Binding SelectedParameter, Mode=TwoWay}"/>
    </Grid>
</UserControl>
