<inf:ViewBase xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:infg="http://infragistics.com/DataPresenter"
              xmlns:inf="clr-namespace:HVTApp.Infrastructure;assembly=HVTApp.Infrastructure"
              xmlns:views="clr-namespace:HVTApp.UI.Views;assembly=HVTApp.UI"
              x:Class="HVTApp.Modules.PlanAndEconomy.Views.ProductionPlanView">

    <Grid>
         <Grid.RowDefinitions>
             <RowDefinition Height="Auto"/>
             <RowDefinition Height="*"/>
         </Grid.RowDefinitions>
         
        <Label Grid.Row="0" Content="Ожидание данных..." VerticalAlignment="Center" HorizontalAlignment="Center">
            <Label.Style>
                <Style>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding ElementName=ContentGrid, Path=Visibility}" Value="Visible">
                        <Setter Property="Label.Visibility" Value="Collapsed"/>
                    </DataTrigger>
                </Style.Triggers>
                </Style>
            </Label.Style>
        </Label>


        <Grid x:Name="ContentGrid" 
              Grid.Row="1" 
              Visibility="{Binding IsLoaded, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
             
            <GroupBox Grid.Column="0" Header="Заводские заказы">
                <views:OrderLookupListView DataContext="{Binding}"/>
            </GroupBox>
            
            <GridSplitter Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Center"  Width="3"/>
           
            <GroupBox Grid.Column="2" Header="Производственный план">
                <Grid>
            
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
            
                    <GroupBox Grid.Row="0" Header="Оборудование в заказе">
                        <infg:XamDataGrid x:Name="ProductionListGrid"
                                          DataSource="{Binding GroupsInOrder}"
                                          SelectedDataItem="{Binding GroupsInOrder.SelectedGroup, Mode=TwoWay}">

                            <infg:XamDataGrid.ContextMenu>
				                <ContextMenu>
					                <MenuItem Header="Export to Excel" Command="{Binding ExportToExcel}" CommandParameter="{Binding PlacementTarget, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ContextMenu}}}"/>
				                </ContextMenu>
			                </infg:XamDataGrid.ContextMenu>

			                <infg:XamDataGrid.FieldLayouts>
				                <infg:FieldLayout>
					                <infg:FieldLayout.Fields>

                                        <infg:TextField AlternateBinding="{Binding Unit.Facility}" BindingType="UseAlternateBinding" Label="Объект" Width="Auto"/>
                                        <infg:TextField AlternateBinding="{Binding Unit.Product.ProductType}" BindingType="UseAlternateBinding" Label="Тип" Width="Auto"/>
                                        <infg:TextField AlternateBinding="{Binding Unit.Product.Designation}" BindingType="UseAlternateBinding" Label="Обозначение" Width="*"/>
                                        <infg:TextField AlternateBinding="{Binding Unit.Order}" BindingType="UseAlternateBinding" Label="З/з" Width="Auto"/>
                                        <!--<infg:TextField AlternateBinding="{Binding OrderPosition, Mode=TwoWay}" BindingType="UseAlternateBinding" Label="Поз." Width="Auto" AllowEdit="True"/>-->
                                        <infg:TextField Name="OrderPosition" Label="Плановая" Width="Auto" AllowEdit="True"/>
                                        <infg:TextField AlternateBinding="{Binding Unit.Specification.Contract.Contragent}" BindingType="UseAlternateBinding" Label="Контрагент" Width="Auto"/>
                                        <infg:TextField AlternateBinding="{Binding Unit.Specification}" BindingType="UseAlternateBinding" Label="Сп.№" Width="Auto"/>
                                        <infg:TextField AlternateBinding="{Binding Unit.Specification.Contract}" BindingType="UseAlternateBinding" Label="Договор" Width="Auto"/>
                                        <infg:TextField AlternateBinding="{Binding Unit.TceRequest}" BindingType="UseAlternateBinding" Label="Запрос в ТСЕ" Width="Auto"/>
                                        <infg:DateTimeField AlternateBinding="{Binding Unit.DeliveryDateExpected}" BindingType="UseAlternateBinding" Label="Требуемая" Width="Auto"/>
                                        <infg:DateTimeField Name="EndProductionPlanDate" Label="Плановая" Width="Auto" AllowEdit="True"/>
                                        <infg:NumericField Name="Amount" Label="Количество" Width="Auto"/>
                                        <infg:Field Name="Groups" IsExpandable="True"/>

                                    </infg:FieldLayout.Fields>
				                </infg:FieldLayout>

			                </infg:XamDataGrid.FieldLayouts>
		                </infg:XamDataGrid>
                    </GroupBox>
            
                    <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="3"/>

                    <GroupBox Grid.Row="2" Header="Оборудование для размещения">
                        <infg:XamDataGrid x:Name="PotentialListGrid"
                                          DataSource="{Binding GroupsPotential}"
                                          SelectedDataItem="{Binding GroupsPotential.SelectedGroup, Mode=TwoWay}">

                            <infg:XamDataGrid.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Export to Excel" Command="{Binding ExportToExcel}" CommandParameter="{Binding PlacementTarget, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ContextMenu}}}"/>
                                </ContextMenu>
                            </infg:XamDataGrid.ContextMenu>

                            <infg:XamDataGrid.FieldLayouts>
                                <infg:FieldLayout>
                                    <infg:FieldLayout.Fields>

                                        <infg:TextField AlternateBinding="{Binding Unit.Facility}" BindingType="UseAlternateBinding" Label="Объект" Width="Auto"/>
                                        <infg:TextField AlternateBinding="{Binding Unit.Product.ProductType}" BindingType="UseAlternateBinding" Label="Тип" Width="Auto"/>
                                        <infg:TextField AlternateBinding="{Binding Unit.Product.Designation}" BindingType="UseAlternateBinding" Label="Обозначение" Width="*"/>
                                        <infg:TextField AlternateBinding="{Binding Unit.Specification.Contract.Contragent}" BindingType="UseAlternateBinding" Label="Контрагент" Width="Auto"/>
                                        <infg:TextField AlternateBinding="{Binding Unit.Specification}" BindingType="UseAlternateBinding" Label="Сп.№" Width="Auto"/>
                                        <infg:TextField AlternateBinding="{Binding Unit.Specification.Contract}" BindingType="UseAlternateBinding" Label="Договор" Width="Auto"/>
                                        <infg:TextField AlternateBinding="{Binding Unit.TceRequest}" BindingType="UseAlternateBinding" Label="Запрос в ТСЕ" Width="Auto"/>
                                        <infg:DateTimeField AlternateBinding="{Binding Unit.DeliveryDateExpected}" BindingType="UseAlternateBinding" Label="Требуемая" Width="Auto"/>
                                        <infg:NumericField Name="Amount" Label="Количество" Width="Auto"/>
                                        <infg:Field Name="Groups" IsExpandable="True"/>

                                    </infg:FieldLayout.Fields>
                                </infg:FieldLayout>

                            </infg:XamDataGrid.FieldLayouts>
                        </infg:XamDataGrid>
                    </GroupBox>

                </Grid>
            </GroupBox>
            
        </Grid>
        

    </Grid>
</inf:ViewBase>

	